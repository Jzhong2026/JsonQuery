<UserControl x:Class="JmesPathWpfDemo.Views.FunctionReferenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://caliburnmicro.com"
             Background="White">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Categories -->
        <Border Grid.Column="0" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1" 
                Background="#FAFAFA">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                          Text="Function Categories"
                          FontSize="16"
                          FontWeight="Bold"
                          Margin="10"
                          Foreground="#333"/>

                <ListBox Grid.Row="1"
                        ItemsSource="{Binding Categories}"
                        SelectedItem="{Binding SelectedCategory}"
                        BorderThickness="0"
                        Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding CategoryName}" 
                                          FontWeight="Bold"
                                          FontSize="14"
                                          Foreground="#0078D4"/>
                                <TextBlock Text="{Binding Description}" 
                                          FontSize="11"
                                          Foreground="#666"
                                          TextWrapping="Wrap"
                                          Margin="0,2,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="Margin" Value="2"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                    <Setter Property="BorderBrush" Value="#0078D4"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <GridSplitter Grid.Column="1" 
                     Width="5" 
                     HorizontalAlignment="Stretch" 
                     Background="#E0E0E0"/>

        <!-- Right Panel: Functions List and Details -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Category Header -->
            <Border Grid.Row="0" 
                    Background="#0078D4" 
                    Padding="15,10"
                    Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="{Binding SelectedCategory.CategoryName}" 
                              FontSize="18"
                              FontWeight="Bold"
                              Foreground="White"/>
                    <TextBlock Text="{Binding SelectedCategory.Description}" 
                              FontSize="12"
                              Foreground="#E0E0E0"
                              Margin="0,3,0,0"/>
                </StackPanel>
            </Border>

            <!-- Functions Details -->
            <ScrollViewer Grid.Row="1" 
                         VerticalScrollBarVisibility="Auto"
                         Padding="5">
                <ItemsControl ItemsSource="{Binding SelectedCategory.Functions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#E0E0E0" 
                                   BorderThickness="1" 
                                   Margin="0,0,0,15"
                                   Background="White">
                                <StackPanel Margin="15">
                                    <!-- Function Name and Syntax -->
                                    <TextBlock Text="{Binding Name}" 
                                              FontSize="16"
                                              FontWeight="Bold"
                                              Foreground="#0078D4"
                                              Margin="0,0,0,5"/>
                                    
                                    <TextBlock Text="{Binding Syntax}" 
                                              FontFamily="Consolas"
                                              FontSize="13"
                                              Background="#F5F5F5"
                                              Padding="10,5"
                                              Margin="0,0,0,10"/>

                                    <!-- Description -->
                                    <TextBlock Text="{Binding Description}" 
                                              FontSize="13"
                                              TextWrapping="Wrap"
                                              Margin="0,0,0,15"
                                              Foreground="#333"/>

                                    <!-- Examples -->
                                    <TextBlock Text="Examples:" 
                                              FontSize="14"
                                              FontWeight="Bold"
                                              Margin="0,0,0,10"
                                              Foreground="#333"/>

                                    <ItemsControl ItemsSource="{Binding Examples}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#F9F9F9" 
                                                       Padding="12"
                                                       Margin="0,0,0,10"
                                                       BorderBrush="#E0E0E0"
                                                       BorderThickness="1">
                                                    <StackPanel>
                                                        <!-- Example Description -->
                                                        <TextBlock Text="{Binding Description}" 
                                                                  FontWeight="SemiBold"
                                                                  FontSize="12"
                                                                  Foreground="#555"
                                                                  Margin="0,0,0,8"/>

                                                        <!-- JSON Data -->
                                                        <TextBlock Text="Input JSON:" 
                                                                  FontSize="11"
                                                                  FontWeight="Bold"
                                                                  Foreground="#666"
                                                                  Margin="0,0,0,3"/>
                                                        <TextBox Text="{Binding JsonData, Mode=OneWay}" 
                                                                FontFamily="Consolas"
                                                                FontSize="11"
                                                                Background="White"
                                                                IsReadOnly="True"
                                                                TextWrapping="Wrap"
                                                                MinHeight="60"
                                                                MaxHeight="150"
                                                                VerticalScrollBarVisibility="Auto"
                                                                Padding="8"
                                                                Margin="0,0,0,8"
                                                                BorderBrush="#CCC"/>

                                                        <!-- Query -->
                                                        <TextBlock Text="Query:" 
                                                                  FontSize="11"
                                                                  FontWeight="Bold"
                                                                  Foreground="#666"
                                                                  Margin="0,0,0,3"/>
                                                        <TextBox Text="{Binding Query, Mode=OneWay}" 
                                                                FontFamily="Consolas"
                                                                FontSize="11"
                                                                Background="White"
                                                                IsReadOnly="True"
                                                                Padding="8"
                                                                Margin="0,0,0,8"
                                                                BorderBrush="#CCC"/>

                                                        <!-- Expected Result -->
                                                        <TextBlock Text="Expected Result:" 
                                                                  FontSize="11"
                                                                  FontWeight="Bold"
                                                                  Foreground="#666"
                                                                  Margin="0,0,0,3"/>
                                                        <TextBox Text="{Binding ExpectedResult, Mode=OneWay}" 
                                                                FontFamily="Consolas"
                                                                FontSize="11"
                                                                Background="White"
                                                                IsReadOnly="True"
                                                                TextWrapping="Wrap"
                                                                Padding="8"
                                                                Margin="0,0,0,10"
                                                                BorderBrush="#CCC"/>

                                                        <!-- Try It Button -->
                                                        <Button Content="Try This Example ?" 
                                                               Width="150"
                                                               HorizontalAlignment="Left"
                                                               Style="{StaticResource SuccessButtonStyle}"
                                                               cal:Message.Attach="[Event Click] = [Action TryExample($dataContext)]"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
