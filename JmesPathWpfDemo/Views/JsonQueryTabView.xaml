<UserControl x:Class="JmesPathWpfDemo.Views.JsonQueryTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://caliburnmicro.com">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Context Menu for Array Nodes -->
        <ContextMenu x:Key="ArrayNodeContextMenu" 
                     cal:Action.TargetWithoutContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="Create New Tab from This Node" 
                      DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      cal:Message.Attach="CreateTabFromNode($dataContext)"/>
            <Separator/>
            <MenuItem Header="Generate todatetime Query" 
                      DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      cal:Message.Attach="GenerateToDateTimeQuery($dataContext)"/>
            <MenuItem Header="Generate join Query..." 
                      DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      cal:Message.Attach="GenerateJoinQuery($dataContext)">
                <MenuItem.Style>
                    <Style TargetType="MenuItem">
                        <Setter Property="IsEnabled" Value="{Binding IsArray}"/>
                    </Style>
                </MenuItem.Style>
            </MenuItem>
            <MenuItem Header="Generate map Query..." 
                      DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      cal:Message.Attach="GenerateMapQuery($dataContext)">
                <MenuItem.Style>
                    <Style TargetType="MenuItem">
                        <Setter Property="IsEnabled" Value="{Binding IsArray}"/>
                    </Style>
                </MenuItem.Style>
            </MenuItem>

            <MenuItem Header="Functions">
                <MenuItem Header="Math (Number)">
                    <MenuItem.Style>
                        <Style TargetType="MenuItem">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PlacementTarget.DataContext.Type, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="Number">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </MenuItem.Style>
                    <MenuItem Header="abs" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'abs')"/>
                    <MenuItem Header="ceil" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'ceil')"/>
                    <MenuItem Header="floor" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'floor')"/>
                </MenuItem>
                <MenuItem Header="Aggregates (Array)">
                    <MenuItem.Style>
                        <Style TargetType="MenuItem">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PlacementTarget.DataContext.IsArray, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </MenuItem.Style>
                    <MenuItem Header="avg" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'avg')"/>
                    <MenuItem Header="sum" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'sum')"/>
                    <MenuItem Header="min" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'min')"/>
                    <MenuItem Header="max" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'max')"/>
                    <MenuItem Header="length" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'length')"/>
                </MenuItem>
                <MenuItem Header="Conversion">
                    <MenuItem Header="to_string" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'to_string')"/>
                    <MenuItem Header="to_number" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'to_number')"/>
                </MenuItem>
                <MenuItem Header="Type Check">
                    <MenuItem Header="type" 
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                             cal:Message.Attach="GenerateFunctionQuery($dataContext, 'type')"/>
                </MenuItem>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Configure Array Sort..." 
                      DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      cal:Message.Attach="ConfigureArraySort($dataContext)">
                <MenuItem.Style>
                    <Style TargetType="MenuItem">
                        <Setter Property="IsEnabled" Value="{Binding IsArray}"/>
                    </Style>
                </MenuItem.Style>
            </MenuItem>
            <MenuItem Header="Clear Sort" 
                      DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      cal:Message.Attach="ClearArraySort($dataContext)">
                <MenuItem.Style>
                    <Style TargetType="MenuItem">
                        <Setter Property="IsEnabled" Value="{Binding HasSortApplied}"/>
                    </Style>
                </MenuItem.Style>
            </MenuItem>
        </ContextMenu>

        <!-- TreeView Item Style -->
        <Style x:Key="TreeNodeStyle" TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="ContextMenu" Value="{StaticResource ArrayNodeContextMenu}"/>
            <!-- Bind Tag to ViewModel so ContextMenu can access it via PlacementTarget.Tag -->
            <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="TreeViewItem_PreviewMouseLeftButtonDown"/>
        </Style>

        <!-- Selected Node Style -->
        <Style x:Key="NodeContainerStyle" TargetType="Border">
            <Setter Property="Padding" Value="3,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="cal:Message.Attach" Value="[Event MouseDown] = [Action OnNodeSelected($dataContext)]"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="#FFE699"/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0F0"/>
                </Trigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsSelected}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FFD966"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title and Buttons Section -->
        <Grid Grid.Row="0" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" 
                       Text="JSON Input:" 
                       FontWeight="Bold" 
                       FontSize="14"
                       VerticalAlignment="Center"/>
            
            <Button Grid.Column="2"
                    Content="Refresh Tree" 
                    Width="100" 
                    Height="28"
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"
                    cal:Message.Attach="RefreshTree">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#28A745"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#218838"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
        
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}">
            <!-- Text Input Tab -->
            <TabItem Header="Text Input">
                <TextBox Text="{Binding JsonInput, UpdateSourceTrigger=PropertyChanged}" 
                         AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" 
                         FontSize="12"
                         Padding="5"
                         Background="#F5F5F5"
                         BorderBrush="#CCCCCC"
                         BorderThickness="1"/>
            </TabItem>
            
            <!-- Tree View Tab -->
            <TabItem Header="Tree View">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" 
                               Padding="5"
                               Background="#E0E0E0"
                               TextWrapping="Wrap">
                        <Run Text="Total Nodes: "/>
                        <Run Text="{Binding JsonTreeNodes.Count, Mode=OneWay}" FontWeight="Bold"/>
                        <Run Text="  |  Tip: Right-click any node to create a new tab or configure sorting"/>
                    </TextBlock>
                    <TreeView x:Name="JsonTreeView"
                              ItemsSource="{Binding JsonTreeNodes}" 
                              Background="#F5F5F5"
                              BorderBrush="#CCCCCC"
                              BorderThickness="1">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem" BasedOn="{StaticResource TreeNodeStyle}"/>
                        </TreeView.ItemContainerStyle>
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <Border Style="{StaticResource NodeContainerStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Key}" 
                                                   Foreground="#0451A5" 
                                                   FontWeight="SemiBold"
                                                   Visibility="{Binding HasKey, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        <TextBlock Text=": " 
                                                   Foreground="Black"
                                                   Visibility="{Binding HasKey, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        <TextBlock Text="{Binding ValueText}" 
                                                   Foreground="#333333"/>
                                    </StackPanel>
                                </Border>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </DockPanel>
            </TabItem>
        </TabControl>

        <!-- Query Section -->
        <TextBlock Grid.Row="2" Text="JMESPath Query:" FontWeight="Bold" FontSize="14" Margin="0,10,0,5"/>
        <DockPanel Grid.Row="3" Margin="0,0,0,10">
            <Button DockPanel.Dock="Right"
                    Content="Execute" 
                    Width="100" 
                    Height="32"
                    Margin="10,0,0,0"
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"
                    cal:Message.Attach="Execute">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#0078D4"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <TextBox Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" 
                     FontFamily="Consolas" 
                     FontSize="12"
                     Padding="5"
                     Height="100"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Background="#F5F5F5"
                     BorderBrush="#CCCCCC"
                     BorderThickness="1"/>
        </DockPanel>

        <!-- Result Section -->
        <Grid Grid.Row="4" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" 
                       Text="Result:" 
                       FontWeight="Bold" 
                       FontSize="14"
                       VerticalAlignment="Center"/>
            
            <Button Grid.Column="2"
                    Content="Create Tab from Result" 
                    Width="160" 
                    Height="28"
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Visibility="{Binding HasValidResult, Converter={StaticResource BooleanToVisibilityConverter}}"
                    cal:Message.Attach="CreateTabFromResult">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#6F42C1"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A32A3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
        
        <TextBox Grid.Row="5" Text="{Binding Result, Mode=OneWay}" 
                 AcceptsReturn="True" 
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" 
                 FontSize="12"
                 Padding="5"
                 Background="#FAFAFA"
                 BorderBrush="#CCCCCC"
                 BorderThickness="1"/>
    </Grid>
</UserControl>
