<UserControl x:Class="JmesPathWpfDemo.Views.XmlWorkspaceView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:cal="http://caliburnmicro.com">
	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
	</UserControl.Resources>

	<Grid Margin="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0"
					Orientation="Horizontal"
					HorizontalAlignment="Center"
					Margin="0,0,0,15">
			<Button Content="XML Query"
					Width="120"
					Margin="0,0,5,0"
					cal:Message.Attach="ShowXmlQuery">
				<Button.Style>
					<Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding CurrentView}" Value="XmlQuery">
								<Setter Property="Background" Value="#005A9E"/>
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Button.Style>
			</Button>

			<Button Content="Query Store"
					Width="120"
					Margin="5,0,5,0"
					cal:Message.Attach="ShowQueryStore">
				<Button.Style>
					<Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding CurrentView}" Value="QueryStore">
								<Setter Property="Background" Value="#005A9E"/>
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Button.Style>
			</Button>

			<Button Content="XPath Function Reference"
					Width="180"
					Margin="5,0,0,0"
					cal:Message.Attach="ShowFunctionReference">
				<Button.Style>
					<Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding CurrentView}" Value="FunctionReference">
								<Setter Property="Background" Value="#005A9E"/>
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Button.Style>
			</Button>
		</StackPanel>

		<Grid Grid.Row="1" Grid.RowSpan="2" Visibility="{Binding XmlQueryViewVisibility}">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<StackPanel Grid.Row="0"
						Orientation="Horizontal"
						HorizontalAlignment="Right"
						Margin="0,0,0,5">
				<Button Content="Save to Store"
						Width="100"
						Style="{StaticResource WarningButtonStyle}"
						cal:Message.Attach="SaveCurrentQuery"/>
			</StackPanel>

			<TabControl Grid.Row="1"
						ItemsSource="{Binding XmlQueryTabs}"
						SelectedItem="{Binding SelectedXmlQueryTab}"
						Style="{StaticResource ModernTabControlStyle}"
						Background="#F8F9FA"
						BorderBrush="#DEE2E6"
						BorderThickness="0,2,0,0">
				<TabControl.ItemContainerStyle>
					<Style TargetType="TabItem" BasedOn="{StaticResource QueryTabStyle}"/>
				</TabControl.ItemContainerStyle>
				<TabControl.ItemTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="{Binding TabTitle}"
									   Margin="0,0,8,0"
									   VerticalAlignment="Center"/>
							<Button Width="18"
									Height="18"
									FontSize="14"
									FontWeight="Bold"
									Padding="0"
									Margin="5,0,0,0"
									Visibility="{Binding CanClose, Converter={StaticResource BooleanToVisibilityConverter}}"
									cal:Message.Attach="[Event Click] = [Action CloseTab($dataContext)]"
									ToolTip="Close Tab">
								<Button.Content>
									<TextBlock Text="&#x00D7;" FontSize="16" FontFamily="Arial"/>
								</Button.Content>
								<Button.Style>
									<Style TargetType="Button">
										<Setter Property="Background" Value="Transparent"/>
										<Setter Property="BorderThickness" Value="0"/>
										<Setter Property="BorderBrush" Value="Transparent"/>
										<Setter Property="Cursor" Value="Hand"/>
										<Setter Property="Foreground" Value="#6C757D"/>
										<Setter Property="Template">
											<Setter.Value>
												<ControlTemplate TargetType="Button">
													<Border Background="{TemplateBinding Background}"
															BorderBrush="{TemplateBinding BorderBrush}"
															BorderThickness="{TemplateBinding BorderThickness}"
															CornerRadius="9"
															Padding="{TemplateBinding Padding}">
														<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
													</Border>
												</ControlTemplate>
											</Setter.Value>
										</Setter>
										<Style.Triggers>
											<Trigger Property="IsMouseOver" Value="True">
												<Setter Property="Background" Value="#DC3545"/>
												<Setter Property="Foreground" Value="White"/>
											</Trigger>
										</Style.Triggers>
									</Style>
								</Button.Style>
							</Button>
						</StackPanel>
					</DataTemplate>
				</TabControl.ItemTemplate>
				<TabControl.ContentTemplate>
					<DataTemplate>
						<ContentControl cal:View.Model="{Binding}"/>
					</DataTemplate>
				</TabControl.ContentTemplate>
			</TabControl>
		</Grid>

		<Grid Grid.Row="1" Grid.RowSpan="2" Visibility="{Binding QueryStoreViewVisibility}">
			<ContentControl cal:View.Model="{Binding QueryStoreViewModel}"/>
		</Grid>

		<Grid Grid.Row="1" Grid.RowSpan="2" Visibility="{Binding FunctionReferenceViewVisibility}">
			<ContentControl cal:View.Model="{Binding FunctionReferenceViewModel}"/>
		</Grid>
	</Grid>
</UserControl>